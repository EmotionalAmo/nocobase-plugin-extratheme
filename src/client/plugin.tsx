import { Plugin } from '@nocobase/client';

export class PluginExtraThemeClient extends Plugin {
  async load() {
    // Filled in Task 7/8: i18n, ThemeInjector for /admin + legacy sign-in, settings page.
  }
}

export default PluginExtraThemeClient;
